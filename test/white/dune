(tests
  (names errors idrefl sym symsym gel natarith sigma stream readback lst vec constants degconst)
	(libraries core parser testutil zarith)
	(instrumentation (backend landmarks --auto)))
(env (_ (flags (:standard -w -26 -w -27))))
