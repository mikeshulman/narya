(tests
  (names church errors idrefl sym symsym gel natarith parametricity churchnat sigma stream letin readback list vec)
	(libraries core types parser testutil)
	(instrumentation (backend landmarks --auto)))
(env (_ (flags (:standard -w -37 -w -30 -w -32))))
