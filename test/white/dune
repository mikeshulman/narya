(tests
  (names errors idrefl sym symsym gel natarith parametricity churchnat sigma stream letin readback lst vec constants)
	(libraries core types parser testutil zarith)
	(instrumentation (backend landmarks --auto)))
(env (_ (flags (:standard -w -37 -w -30 -w -32 -w -26 -w -27))))
